name: Build Application and Make Release

on:
  push:
    tags:
      - "*"

permissions: {}

jobs:
  build_release:
    name: Build Release
    uses: ./.github/workflows/build.yml
    permissions:
      contents: read
      # Required for Azure Trusted Signing
      id-token: write
      # Required for vcpkg binary cache
      packages: write
    with:
      build-type: Release
      environment: Release
    secrets: inherit

  create_release:
    needs: build_release
    permissions:
      contents: write
    runs-on: ubuntu-slim
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: "true"
          path: "SteelLauncher-source"
      - name: Download artifacts
        uses: actions/download-artifact@v8
      - name: Grab and store version
        run: |
          tag_name=$(echo ${{ github.ref }} | grep -oE "[^/]+$")
          echo "VERSION=$tag_name" >> $GITHUB_ENV
      - name: Package artifacts properly
        run: |
          mv ${{ github.workspace }}/SteelLauncher-source SteelLauncher-${{ env.VERSION }}
          mv SteelLauncher-Linux-Qt6-Portable*/SteelLauncher-portable.tar.gz SteelLauncher-Linux-Qt6-Portable-${{ env.VERSION }}.tar.gz
          mv SteelLauncher-Linux-aarch64-Qt6-Portable*/SteelLauncher-portable.tar.gz SteelLauncher-Linux-aarch64-Qt6-Portable-${{ env.VERSION }}.tar.gz
          mv SteelLauncher-*.AppImage/SteelLauncher-*-x86_64.AppImage SteelLauncher-Linux-x86_64.AppImage
          mv SteelLauncher-*.AppImage.zsync/SteelLauncher-*-x86_64.AppImage.zsync SteelLauncher-Linux-x86_64.AppImage.zsync
          mv SteelLauncher-*.AppImage/SteelLauncher-*-aarch64.AppImage SteelLauncher-Linux-aarch64.AppImage
          mv SteelLauncher-*.AppImage.zsync/SteelLauncher-*-aarch64.AppImage.zsync SteelLauncher-Linux-aarch64.AppImage.zsync
          mv SteelLauncher-macOS*/SteelLauncher.zip SteelLauncher-macOS-${{ env.VERSION }}.zip
          mv SteelLauncher-macOS*/SteelLauncher.dmg SteelLauncher-macOS-${{ env.VERSION }}.dmg

          tar --exclude='.git' -czf SteelLauncher-${{ env.VERSION }}.tar.gz SteelLauncher-${{ env.VERSION }}

          for d in SteelLauncher-Windows-MSVC*; do
            cd "${d}" || continue
            LEGACY="$(echo -n ${d} | grep -o Legacy || true)"
            ARM64="$(echo -n ${d} | grep -o arm64 || true)"
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="SteelLauncher-Windows-MSVC"
            test -z "${LEGACY}" || NAME="${NAME}-Legacy"
            test -z "${ARM64}" || NAME="${NAME}-arm64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv SteelLauncher-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

          for d in SteelLauncher-Windows-MinGW-w64*; do
            cd "${d}" || continue
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="SteelLauncher-Windows-MinGW-w64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv SteelLauncher-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

          for d in SteelLauncher-Windows-MinGW-arm64*; do
            cd "${d}" || continue
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="SteelLauncher-Windows-MinGW-arm64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv SteelLauncher-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

      - name: Create release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          tag_name: ${{ github.ref }}
          name: Prism Launcher ${{ env.VERSION }}
          draft: true
          prerelease: false
          fail_on_unmatched_files: true
          files: |
            SteelLauncher-Linux-x86_64.AppImage
            SteelLauncher-Linux-x86_64.AppImage.zsync
            SteelLauncher-Linux-aarch64.AppImage
            SteelLauncher-Linux-aarch64.AppImage.zsync
            SteelLauncher-Linux-Qt6-Portable-${{ env.VERSION }}.tar.gz
            SteelLauncher-Linux-aarch64-Qt6-Portable-${{ env.VERSION }}.tar.gz
            SteelLauncher-Windows-MinGW-w64-${{ env.VERSION }}.zip
            SteelLauncher-Windows-MinGW-w64-Portable-${{ env.VERSION }}.zip
            SteelLauncher-Windows-MinGW-w64-Setup-${{ env.VERSION }}.exe
            SteelLauncher-Windows-MinGW-arm64-${{ env.VERSION }}.zip
            SteelLauncher-Windows-MinGW-arm64-Portable-${{ env.VERSION }}.zip
            SteelLauncher-Windows-MinGW-arm64-Setup-${{ env.VERSION }}.exe
            SteelLauncher-Windows-MSVC-arm64-${{ env.VERSION }}.zip
            SteelLauncher-Windows-MSVC-arm64-Portable-${{ env.VERSION }}.zip
            SteelLauncher-Windows-MSVC-arm64-Setup-${{ env.VERSION }}.exe
            SteelLauncher-Windows-MSVC-${{ env.VERSION }}.zip
            SteelLauncher-Windows-MSVC-Portable-${{ env.VERSION }}.zip
            SteelLauncher-Windows-MSVC-Setup-${{ env.VERSION }}.exe
            SteelLauncher-macOS-${{ env.VERSION }}.zip
            SteelLauncher-macOS-${{ env.VERSION }}.dmg
            SteelLauncher-${{ env.VERSION }}.tar.gz
